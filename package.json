{
  "name": "@ghom/programmatic-seo",
  "version": "0.1.1",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "description": "Programmatic S.E.O tools for NodeJS",
  "prettier": {
    "semi": false,
    "endOfLine": "lf"
  },
  "scripts": {
    "format": "prettier --write src tsconfig.* tests",
    "build": "tsc",
    "test": "npm run build && jest tests/test.js --detectOpenHandles",
    "prepublishOnly": "npm run format && npm test"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "jest": "^29.7.0",
    "prettier": "^2.5.1",
    "typescript": "^4.5.5"
  }
}
